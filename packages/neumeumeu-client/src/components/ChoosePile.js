import React from "react";
import { pure } from "recompose";

const ChoosePile = () => (
  <div className="choose-pile">
    <span className="choose-pile__text">Would you kindly choose a pile ?</span>
  </div>
);

export default pure(ChoosePile);
